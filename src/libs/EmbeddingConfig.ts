/**
 * Shared embedding configuration.
 * Keep model and vector dimensions in one place to avoid mismatches.
 */

export const EMBEDDING_MODEL = 'mistral-embed';
export const EMBEDDING_DIMENSION = 1024;
